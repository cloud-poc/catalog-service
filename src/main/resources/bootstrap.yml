spring.application.name: catalog-service
spring.cloud.config:
  uri:
  - http://localhost:8888
server.port: 9001
management:
  endpoints:
    web:
      exposure:
        include: '*'

spring:
  profiles:
    active:
    - default
